{
  "name": "papra-email-proxy",
  "type": "module",
  "version": "1.0.3",
  "packageManager": "pnpm@9.15.9",
  "description": "Cloudflare Email Worker that forwards inbound mail to Papra, with optional Cloudflare Zero Trust (Access) service-token support for protected origins",
  "author": "Corentin Thomasset <corentinth@proton.me> (https://corentin.tech)",
  "license": "MIT",
  "homepage": "https://github.com/Robert27/papra-email-proxy",
  "repository": {
    "type": "git",
    "url": "https://github.com/Robert27/papra-email-proxy"
  },
  "bugs": {
    "url": "https://github.com/Robert27/papra-email-proxy/issues"
  },
  "keywords": [
    "email",
    "proxy",
    "papra",
    "document",
    "ingestion",
    "cloudflare",
    "workers",
    "zero-trust",
    "cloudflare-access",
    "service-token"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "cf-types": "wrangler types",
    "typecheck": "wrangler types --check && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --reporter=default --reporter=junit --outputFile.junit=test-report.junit.xml",
    "build": "wrangler build",
    "release": "bumpp --commit --tag --push"
  },
  "dependencies": {
    "@crowlog/logger": "^2.2.2",
    "@owlrelay/webhook": "^0.0.4",
    "postal-mime": "^2.7.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@vitest/coverage-v8": "^4.1.11",
    "bumpp": "^11.1.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.0"
  }
}
